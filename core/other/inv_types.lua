return {
    chest = defines.inventory.chest,
    fuel = defines.inventory.fuel,

    turret_ammo = defines.inventory.turret_ammo,
    artillery_ammo = defines.inventory.artillery_turret_ammo,
    artillery_wagon_ammo = defines.inventory.artillery_wagon_ammo,

    rocket_silo_rocket = defines.inventory.rocket_silo_rocket,
    rocket_silo_in = defines.inventory.rocket_silo_input,
    rocket_silo_out = defines.inventory.rocket_silo_output,

    furnace_in = defines.inventory.furnace_source,
    furnace_out = defines.inventory.furnace_result,

    assembler_in = defines.inventory.assembling_machine_input,
    assembler_out = defines.inventory.assembling_machine_output,

    lab = defines.inventory.lab_input,

    car = defines.inventory.car_trunk,
    tank = defines.inventory.car_trunk,
    
    wagon = defines.inventory.cargo_wagon,

    lab_modules = defines.inventory.lab_modules,
    furnace_modules = defines.inventory.furnace_modules,
    assembler_modules = defines.inventory.assembling_machine_modules,
    drill_modules = defines.inventory.mining_drill_modules,
    rocket_modules = defines.inventory.rocket_silo_modules,
    beacon_modules = defines.inventory.beacon_modules
}
